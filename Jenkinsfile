pipeline {
    agent any

    stages {
        stage('course') {
            steps {
                script {
                    def course = "K8S"

                    if (course == "K8S") {
                        echo "This course is ${course}"
                    } else {
                        echo "This course is not ${course}"
                    }
                }
            }
        }
    }
}
